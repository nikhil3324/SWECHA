{#
/**
 * @file
 * Template for the main page.
 */
#}

<!-- Sidebar -->
<nav id="menu" class="navbar navbar-inverse navbar-fixed-top">
  {% if page.navbar_left %}
    <div class="navbar-left">
      {{ page.navbar_left }}
    </div>
  {% endif %}
</nav>

<!-- /#sidebar-wrapper -->

<div id="page-content-wrapper">
    <div class="slideout-dimmer"></div>
    <button type="button" class="hamburger is-closed animated fadeInLeft toggle-button" data-toggle="offcanvas">
    <span class="hamb-top"></span>
    <span class="hamb-middle"></span>
    <span class="hamb-bottom"></span>
  </button>
  <div class="container">
    <div class="row">
      {% if page.head %}
        <div class="region-head col-md-12">
          {{ page.head }}
        </div>
      {% endif %}
      {% if page.map %}
        <div class="region-map col-md-12">
          {{ page.map }}
        </div>
      {% endif %}

      {% if page.sidebar %}
        <div class="region-content col-md-8">
      {% endif %}
      {% if page.sidebar is empty %}
        <div class="region-content col-md-12">
      {% endif %}
          {{ page.content }}
        </div>
      {% if page.sidebar %}
      <div class="region-sidebar col-md-4">
        {{ page.sidebar }}
      </div>
      {% endif %}
    </div>
  </div>
  {% if page.footer %}
    <footer id="footer" class="footer" role="footer">
      <div class="container">
        {{ page.footer }}
      </div>
    </footer>
  {% endif %}
</div>

